# Kormopulse frontend

React 18 + Vite client for Kormopulse.

```bash
npm install
npm run dev
```

The client uses `VITE_API_URL`, defaulting to `http://localhost:8000/api`. Copy `.env.example` to `.env` to override it.
